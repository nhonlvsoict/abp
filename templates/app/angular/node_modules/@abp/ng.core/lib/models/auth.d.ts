export interface LoginParams {
    username: string;
    password: string;
    rememberMe?: boolean;
    redirectUrl?: string;
}
export interface LoginCaptchaParams {
    username: string;
    password: string;
    rememberMe?: boolean;
    redirectUrl?: string;
    recaptcha: string;
    twoFactorCode: string
}